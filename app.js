const express = require('express');
const fs = require('fs');
const readLine = require('readline');

const commandLineInputs = process.argv;

console.log("working")
const app = express();
module.exports = app;